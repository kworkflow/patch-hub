/// Visual theme configuration for the UI actor.
///
/// Holds styling and layout policy. Kept minimal in phase 11; extended when
/// per-theme rendering is needed.
#[derive(Default, Clone, Debug)]
pub struct UiTheme;
